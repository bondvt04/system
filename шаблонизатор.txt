// если нет оболочки то просто примет {{dataOne}}

/*
Возвращается поле шаблон где {{data}} - будут заменены на соответсвующие свойства шаблоны, совпадающие по именам с атрибутами
если атрибут с этим именем массив то шаблоен будет повторён несколько раз по количеству элементов в массиве.

 {data}  заменяется на соответсвующее свойство объекта элемента массива

 шаблон заменит соответсвенно помеченный участок в шаблоне родителя

  если просто имя свойства без this и прочего - свойство атрибутов совпадающее с именем свойства закидывается во временный объект и от туда получаются данные
        {dataOne} -создать временный элемент итеративно (если представлен массивом )либо разово
        {dataOne} -заменить на него
        {dataOne.имяСвойства} -заменить на его свойство в данной итерации

*/

